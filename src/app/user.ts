import { FormGroup } from "@angular/forms";

//  class User<T> {
//     id!: number;
//     editing!: boolean;
//     currentData?: T;
//     originalData!: T;
//     source!: User<T>;
//     validator!: FormGroup; // Used only in reactive forms.
//  // Used only in reactive forms.
  
//     delete(): void;
//     confirmEditCreate(): boolean;
//     startEdit(): void;
//     cancelOrDelete(): void;
//     isValid(): boolean; // Used only in reactive forms.
//   }